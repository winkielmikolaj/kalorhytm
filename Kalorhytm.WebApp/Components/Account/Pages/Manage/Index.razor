@page "/Account/Manage"

@using System.ComponentModel.DataAnnotations
@using Microsoft.AspNetCore.Identity
@using Kalorhytm.Infrastructure
@using Microsoft.AspNetCore.Http
@using System.IO 

@inject UserManager<ApplicationUser> UserManager
@inject SignInManager<ApplicationUser> SignInManager
@inject IdentityUserAccessor UserAccessor
@inject IdentityRedirectManager RedirectManager

<PageTitle>Twój Profil</PageTitle>

@using Kalorhytm.WebApp.Components.Shared

<Card>
    <h3 class="text-2xl font-semibold text-white mb-6">Twój Profil</h3>
    <StatusMessage Message="@message" />
    
    <EditForm Model="Input" FormName="profile" OnValidSubmit="OnValidSubmitAsync" enctype="multipart/form-data">
        <DataAnnotationsValidator />
        <ValidationSummary class="text-red-400 text-sm mb-4 p-3 bg-red-500/10 rounded-lg border border-red-500/30" role="alert" />
        
        <div class="space-y-4 max-w-md">
            <div class="mb-4 text-center">
                @if (Input.ProfilePicture != null && Input.ProfilePicture.Length > 0)
                {
                    <img src="data:image/png;base64,@Convert.ToBase64String(Input.ProfilePicture)"
                         class="rounded-full mb-2 mx-auto"
                         style="width: 150px; height: 150px; object-fit: cover; border: 3px solid rgb(34 197 94);" 
                         alt="Zdjęcie profilowe" />
                }
                else
                {
                    <div class="rounded-full mb-2 bg-gray-600 text-white flex items-center justify-center mx-auto"
                         style="width: 150px; height: 150px; font-size: 3rem;">
                        👤
                    </div>
                }

                <div class="mt-3">
                    <label for="imageUpload" class="block text-sm font-medium text-gray-300 mb-2">Wybierz nowe zdjęcie</label>
                    <input type="file" id="imageUpload" name="Input.ImageFile" 
                           class="w-full bg-gray-600 text-white rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-green-500" 
                           accept=".png,.jpg,.jpeg" />
                    <ValidationMessage For="() => Input.ImageFile" class="text-red-400 text-sm mt-1" />
                </div>
            </div>

            <div>
                <label for="username" class="block text-sm font-medium text-gray-300 mb-2">Nazwa użytkownika</label>
                <input type="text" 
                       value="@username" 
                       id="username" 
                       class="w-full bg-gray-600 text-gray-400 rounded-lg px-3 py-2 text-sm cursor-not-allowed" 
                       placeholder="Nazwa użytkownika" 
                       disabled />
            </div>

            <div>
                <label for="Input.FirstName" class="block text-sm font-medium text-gray-300 mb-2">Imię</label>
                <input type="text" 
                       @bind="Input.FirstName" 
                       id="Input.FirstName" 
                       class="w-full bg-gray-600 text-white rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-green-500 focus:ring-offset-2 focus:ring-offset-gray-700 transition-all duration-200" 
                       placeholder="Imię" />
                <ValidationMessage For="() => Input.FirstName" class="text-red-400 text-sm mt-1" />
            </div>

            <div>
                <label for="Input.LastName" class="block text-sm font-medium text-gray-300 mb-2">Nazwisko</label>
                <input type="text" 
                       @bind="Input.LastName" 
                       id="Input.LastName" 
                       class="w-full bg-gray-600 text-white rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-green-500 focus:ring-offset-2 focus:ring-offset-gray-700 transition-all duration-200" 
                       placeholder="Nazwisko" />
                <ValidationMessage For="() => Input.LastName" class="text-red-400 text-sm mt-1" />
            </div>

            <div>
                <label for="Input.PhoneNumber" class="block text-sm font-medium text-gray-300 mb-2">Numer telefonu</label>
                <input type="tel" 
                       @bind="Input.PhoneNumber" 
                       id="Input.PhoneNumber" 
                       class="w-full bg-gray-600 text-white rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-green-500 focus:ring-offset-2 focus:ring-offset-gray-700 transition-all duration-200" 
                       placeholder="Numer telefonu" />
                <ValidationMessage For="() => Input.PhoneNumber" class="text-red-400 text-sm mt-1" />
            </div>
            
            <Button ButtonType="ButtonType.Submit" AdditionalClass="w-full">
                Zapisz zmiany
            </Button>
        </div>
    </EditForm>
</Card>

@code {
    private ApplicationUser user = default!;
    private string? username;
    private string? message;

    [CascadingParameter]
    private HttpContext HttpContext { get; set; } = default!;

    [SupplyParameterFromForm]
    private InputModel Input { get; set; } = new();

    protected override async Task OnInitializedAsync()
    {
        user = await UserAccessor.GetRequiredUserAsync(HttpContext);
        username = await UserManager.GetUserNameAsync(user);

        if (Input.FirstName == null && Input.LastName == null && Input.PhoneNumber == null)
        {
            Input.PhoneNumber = await UserManager.GetPhoneNumberAsync(user);
            Input.FirstName = user.FirstName;
            Input.LastName = user.LastName;
            Input.ProfilePicture = user.ProfilePicture;
        }
    }

    private async Task OnValidSubmitAsync()
    {
        bool hasChanges = false;

        // 1. OBSŁUGA ZDJĘCIA
        // Jeśli plik przeszedł walidację (Attributes w InputModel), zapisujemy go
        if (Input.ImageFile != null)
        {
            using var memoryStream = new MemoryStream();
            await Input.ImageFile.CopyToAsync(memoryStream);
            
            user.ProfilePicture = memoryStream.ToArray();
            // Wymuszamy aktualizację użytkownika, by zapisać zdjęcie
            var photoResult = await UserManager.UpdateAsync(user);
            if (!photoResult.Succeeded)
            {
                message = "Błąd: Nie udało się zaktualizować zdjęcia.";
                return;
            }
            hasChanges = true;
        }

        // 2. OBSŁUGA DANYCH TEKSTOWYCH
        if (Input.FirstName != user.FirstName || Input.LastName != user.LastName)
        {
            user.FirstName = Input.FirstName;
            user.LastName = Input.LastName;
            var updateResult = await UserManager.UpdateAsync(user);
            if (!updateResult.Succeeded)
            {
                message = "Błąd: Nie udało się zaktualizować danych osobowych.";
                return;
            }
            hasChanges = true;
        }

        var phoneNumber = await UserManager.GetPhoneNumberAsync(user);
        if (Input.PhoneNumber != phoneNumber)
        {
            var setPhoneResult = await UserManager.SetPhoneNumberAsync(user, Input.PhoneNumber);
            if (!setPhoneResult.Succeeded)
            {
                RedirectManager.RedirectToCurrentPageWithStatus("Błąd: Nie udało się ustawić numeru telefonu.", HttpContext);
                return;
            }
            hasChanges = true;
        }

        if (hasChanges)
        {
            await SignInManager.RefreshSignInAsync(user);
            RedirectManager.RedirectToCurrentPageWithStatus("Twój profil został zaktualizowany", HttpContext);
        }
        else
        {
             RedirectManager.RedirectToCurrentPageWithStatus("Nie wprowadzono żadnych zmian", HttpContext);
        }
    }

    private sealed class InputModel
    {
        // WALIDACJA NUMERU TELEFONU
        [Required(ErrorMessage = "Numer telefonu jest wymagany.")]
        // Regex: Tylko cyfry, spacje, plus i myślnik. Zakaz liter.
        [RegularExpression(@"^[\d\s\-\+]+$", ErrorMessage = "Numer telefonu może zawierać tylko cyfry oraz znaki +, - lub spacje (bez liter).")]
        [Display(Name = "Numer telefonu")]
        public string? PhoneNumber { get; set; }

        // WALIDACJA IMIENIA
        [Required(ErrorMessage = "Imię jest wymagane.")]
        [StringLength(50, MinimumLength = 2, ErrorMessage = "Imię musi mieć od 2 do 50 znaków.")]
        // Regex: Litery (w tym polskie), spacje i myślniki. Zakaz cyfr i kropek.
        [RegularExpression(@"^[a-zA-ZąćęłńóśźżĄĆĘŁŃÓŚŹŻ\s\-]+$", ErrorMessage = "Imię może zawierać tylko litery (bez cyfr i znaków specjalnych jak kropki).")]
        [Display(Name = "Imię")]
        public string? FirstName { get; set; }

        // WALIDACJA NAZWISKA
        [Required(ErrorMessage = "Nazwisko jest wymagane.")]
        [StringLength(50, MinimumLength = 2, ErrorMessage = "Nazwisko musi mieć od 2 do 50 znaków.")]
        // Regex: To samo co imię
        [RegularExpression(@"^[a-zA-ZąćęłńóśźżĄĆĘŁŃÓŚŹŻ\s\-]+$", ErrorMessage = "Nazwisko może zawierać tylko litery (bez cyfr i znaków specjalnych jak kropki).")]
        [Display(Name = "Nazwisko")]
        public string? LastName { get; set; }

        // To pole służy tylko do wyświetlania obecnego zdjęcia (z bazy)
        public byte[]? ProfilePicture { get; set; }

        // WALIDACJA PLIKU
        [Display(Name = "Zdjęcie profilowe")]
        [MaxFileSize(5 * 1024 * 1024, ErrorMessage = "Maksymalny rozmiar pliku to 5MB.")]
        [AllowedExtensions(new string[] { ".jpg", ".jpeg", ".png" }, ErrorMessage = "Dozwolone są tylko pliki .jpg, .jpeg, .png")]
        public IFormFile? ImageFile { get; set; }
    }

    // --- KLASY POMOCNICZE DO WALIDACJI PLIKU (Backend) ---

    // Sprawdza rozmiar pliku
    public class MaxFileSizeAttribute : ValidationAttribute
    {
        private readonly int _maxFileSize;
        public MaxFileSizeAttribute(int maxFileSize)
        {
            _maxFileSize = maxFileSize;
        }

        protected override ValidationResult? IsValid(object? value, ValidationContext validationContext)
        {
            var file = value as IFormFile;
            if (file != null)
            {
                if (file.Length > _maxFileSize)
                {
                    return new ValidationResult(ErrorMessage ?? $"Maksymalny rozmiar pliku to {_maxFileSize / (1024 * 1024)}MB.");
                }
            }
            return ValidationResult.Success;
        }
    }

    // Sprawdza rozszerzenie pliku
    public class AllowedExtensionsAttribute : ValidationAttribute
    {
        private readonly string[] _extensions;
        public AllowedExtensionsAttribute(string[] extensions)
        {
            _extensions = extensions;
        }

        protected override ValidationResult? IsValid(object? value, ValidationContext validationContext)
        {
            var file = value as IFormFile;
            if (file != null)
            {
                var extension = Path.GetExtension(file.FileName).ToLowerInvariant();
                if (!_extensions.Contains(extension))
                {
                    return new ValidationResult(ErrorMessage ?? $"Dozwolone rozszerzenia: {string.Join(", ", _extensions)}");
                }
            }
            return ValidationResult.Success;
        }
    }
}